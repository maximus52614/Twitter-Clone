<h1> Users </h1>
<table>
 <thead>
  <th>Email</th>
  <th>Created At</th>
 </thead>
 <tbody>
  <% @users.each do |user| %>
   <tr>
    <td><%= user.email %></td>
    <td><%= user.created_at.to_s(:long) %></td>
   </tr>
  <% end %>
 </tbody>
</table>
