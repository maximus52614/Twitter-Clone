-# %h2 Log in
-# = form_for(User.new, as: "user", url: session_path("user")) do |f|
-#   .field
-#     = f.label :email
-#     = f.email_field :email,
-#       autofocus: true
-#
-#   .field
-#     = f.label :password
-#     = f.password_field :password,
-#       autocomplete: "off"
-#
-#   .field
-#     = f.check_box :remember_me
-#     = f.label :remember_me
-#
-#   .actions
-#     = f.submit "Log in"
-#
-#   %ul= link_to "Don't have an Account?", new_user_registration_path
-#   %ul= link_to "Forgot your password?", new_user_password_path
.container{style: "margin-top: 30px"}
  %h2 Log In
  = form_for(User.new, as: "user", url: session_path("user")) do |f|
    .container.col-md-6.col-md-offset-3
      .form-group
        -# = f.label :email
        = f.email_field :email, class: 'form-control', placeholder: 'Email',
          autofocus: true

      .form-group
        -# = f.label :password
        = f.password_field :password, class: 'form-control', placeholder: 'Password',
          autocomplete: "off"

      .form-group
        = f.check_box :remember_me
        = f.label :remember_me

      .actions
        = f.submit "Log in", class: 'btn btn-success'
      %ul
        %li= link_to "Don't have an Account?", new_user_registration_path
        %li= link_to "Forgot your password?", new_user_password_path
